#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <windows.h>


int main(void){
    SetConsoleOutputCP(CP_UTF8);
    SetConsoleCP(CP_UTF8);

    int *pr;
    printf("%d",sizeof(pr));

    // Если значение равно 4 байтам, то это х32 разрядная система
    // Если значение равно 8 байтам, как в нашем случае, то это х64 разрядная система


}